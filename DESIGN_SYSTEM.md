# 어디있을까? — 디자인 시스템 (v1)

> 톤앤매너: **코럴 트래블** — 사진·탐색이 중심인 여행 앱에 어울리는 활기차고 따뜻한 무드.
> 컴포넌트 시안(라이브 프리뷰)은 별도 Artifact로 확인. 이 문서는 확정된 토큰과 사용 규칙을 코드 구현 시 그대로 재사용하기 위한 기준이다.

## 1. 컬러 토큰

| 토큰 | HEX | 용도 |
|---|---|---|
| `bg` | `#FFFBF8` | 기본 배경 |
| `bg-raised` | `#FFF4EC` | 카드/패널 배경 |
| `text` | `#2B2320` | 기본 텍스트 |
| `text-muted` | `#6B5A4E` | 보조 텍스트, 캡션 |
| `border` | `#ECDFD3` | 헤어라인, 구분선 |
| `primary` | `#F2704F` | 코랄 — 메인 CTA, 강조 아이콘 |
| `primary-hover` | `#DC5C3B` | Primary 버튼 hover/active |
| `accent` | `#1E8A82` | 티탈 — 보조 CTA, 활성 탭, 링크 |
| `accent-soft` | `#E4F3F1` | 티탈 배경 틴트 |

### 정확도 배지 전용 (PRD.md §7 4단계와 매핑)

| 구간 | 판정 | 전경색 | 배경색 |
|---|---|---|---|
| 90~100% | 정확 일치 | `#2E9E5B` | `#E4F3EA` |
| 70~89% | 일치 가능성 높음 | `#1E8A82` (accent) | `#E4F3F1` (accent-soft) |
| 40~69% | 유사한 후보 | `#C1502F` | `#FDEAE4` |
| 0~39% | 낮은 확신 | `#6B5A4E` (text-muted) | `#F1ECE7` |

지도 마커(F-06/F-07)는 새 색을 추가하지 않고 위 팔레트를 그대로 재사용한다: 관광지 = `accent`, 음식점 = `primary`, 놀거리 = 정확도 배지의 성공색(`#2E9E5B`).

## 2. 타이포그래피

- **디스플레이(영문 라벨/워드마크 전용)**: `"Avenir Next", "Century Gothic", "Futura", sans-serif` — 굵은 지오메트릭 산세리프. 컴포넌트 카테고리 라벨처럼 영문 짧은 텍스트에만 사용.
- **본문/UI(한글 포함 실제 카피)**: `-apple-system, BlinkMacSystemFont, "Apple SD Gothic Neo", "Malgun Gothic", "Noto Sans KR", sans-serif`
- **수치/정확도(%) 등 숫자 정렬이 필요한 곳**: 본문 스택 + `font-variant-numeric: tabular-nums`

두 서체 모두 외부 웹폰트 로드 없이 시스템 폰트 스택만 사용한다 — 폰트 로드 실패로 인한 화면 깨짐을 원천 차단.

## 3. 형태 규칙

- 버튼: pill(완전 둥근 모서리) — 모바일에서 터치 타깃임을 명확히 함
- 카드/패널: `border-radius: 12px`
- 입력 필드: `border-radius: 8px`
- 배지/칩: pill

## 4. 문구(카피) 원칙

`Design.pdf` 와이어프레임의 문구는 레이아웃을 설명하기 위한 placeholder에 가깝다. **문구는 기능(PRD.md)에 맞게 자유롭게 다듬어도 되지만, 기능의 의도 자체를 바꾸는 수정은 금지**한다 — 특히 정확도 구간별 안내 문구는 신뢰도 정보를 반드시 전달해야 한다 (PRD.md §7).

## 5. 공용 컴포넌트 목록

버튼(Primary/Secondary/Disabled), 정확도 배지(4단계), 입력 필드(기본/에러), 이미지 업로드 카드, 장소 결과 카드, 상세정보 리스트 행, 하단 내비게이션, 탭 스위처, 필터 칩, 지도 마커 범례. 각 컴포넌트의 시각 시안은 Artifact(HTML)로 별도 제공.
