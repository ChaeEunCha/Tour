# 어디있을까 (Where Is It) — PRD

> 출처: `어디있을까_기획서_기능명세서_통합본.docx` (v1.0 통합본)

여행 중 또는 여행 후 마주치는 사진 한 장으로 그 장소를 다시 찾을 수 있도록 돕는 서비스. 정확히 일치하는 관광지를 찾아주거나, 일치하는 곳이 없을 경우 분위기가 유사한 관광지를 추천하고, 이어서 주변 먹거리·놀거리 정보와 지도, 저장(북마크) 기능까지 하나의 흐름으로 제공한다.

개발 조건: 유료 API 사용 불가 · 소셜 로그인 미사용(이메일 로그인만) · DB는 Supabase로 확정

---

## 1. 문제 정의

### 1.1 배경
- SNS 피드나 지인이 보내준 사진 속 장소가 어디인지 몰라 궁금해하는 경험이 반복적으로 발생함
- 스크린샷·캡처·재전송 과정에서 EXIF(위치) 메타데이터가 손실되는 경우가 많음
- 장소명이나 지역명을 전혀 모르는 상태에서는 키워드 검색 자체가 성립하지 않음

### 1.2 5 Whys 분석
1. 사람들은 왜 사진 속 장소를 못 찾아 답답해할까? → 사진에 위치 정보(GPS·태그)가 없는 경우가 많기 때문
2. 왜 위치 정보가 없을까? → SNS 캡처·지인 공유·스크린샷 과정에서 원본 메타데이터가 삭제되기 때문
3. 왜 이것이 문제가 될까? → 마음에 든 장소를 재방문하거나 지인에게 추천하고 싶은데 방법이 없어 결국 포기하게 됨
4. 왜 키워드 검색으로 대체할 수 없을까? → 장소명·지역명 등 최소한의 정보가 없어 검색어 자체를 만들 수 없기 때문
5. 왜 이 상황이 반복적으로 발생할까? → SNS·여행 콘텐츠 소비가 늘며 "여기 어디예요?" 식의 질문이 습관적으로 발생하지만, 사진을 입력값으로 장소를 역추적해주는 전용 도구가 없기 때문

**근본 원인(Root Cause):** 사진 한 장을 근거로 장소를 역추적할 수 있는 도구의 부재

### 1.3 가설 및 검증 방법
**가설:** 여행 사진 한 장을 업로드하면 정확한 장소 혹은 유사한 장소를 추천해주고, 이어서 주변 정보까지 제공한다면 사용자의 재방문·추천·탐색 의사결정에 실질적으로 도움이 될 것이다.

- 정성 조사: 여행 사진을 보고 장소를 다시 찾아본 경험이 있는지, 빈도와 상황을 10~15명 대상 인터뷰
- 정량 조사: SNS·커뮤니티 내 "여기 어디예요?" 유형 댓글·질문 빈도 샘플링
- 검증 지표(가설 채택 기준): 최근 3개월 내 유사 경험 응답률 60% 이상, 서비스 사용 의향 50% 이상

---

## 2. 페르소나

### 2.1 핵심 페르소나 — 김여진 (28세, 직장인)
- **특징:** 마케팅 회사 재직, 국내 여행을 자주 다니고 인스타그램 사용 빈도가 높음
- **Pain Point:** 피드에서 본 예쁜 사진에 장소 태그가 없어 못 찾음 / 여행 후 사진첩을 정리하다 어디서 찍었는지 기억나지 않는 사진을 발견함
- **목표:** 사진 한 장으로 빠르게 장소를 확인하고, 근처 맛집·놀거리까지 한 번에 여행 계획을 세우고 싶음

### 2.2 보조 페르소나 — 박준혁 (32세, 여행 콘텐츠 크리에이터)
- **특징:** 여행 블로그·유튜브 운영, 방문지가 많고 콘텐츠 아카이빙이 필요함
- **Pain Point:** 팔로워의 "여기 어디예요?" DM에 일일이 답하기 번거로움 / 다녀온 장소를 다시 정리하는 데 시간이 많이 소요됨
- **목표:** 사진 기반 장소 인식으로 답변 시간을 줄이고, 저장 기능으로 방문지를 콘텐츠 소재로 아카이빙하고 싶음

---

## 3. 문제 해결 방안 (How Might We)

| HMW | 해결 방향 |
|---|---|
| 사진 한 장으로 정확한 장소를 어떻게 찾아줄 수 있을까? | 이미지 임베딩 기반 유사도 검색 활용 |
| 정확히 일치하는 곳이 없다면 어떻게 도와줄까? | 유사도 상위 후보를 항상 반환, 정확도(%)로 신뢰도 전달 |
| 장소를 찾은 이후의 행동(방문 계획)을 어떻게 도와줄까? | 상세정보 + 지도 + 주변 먹거리·놀거리를 하나의 흐름으로 연결 |
| 찾은 장소를 나중에 다시 보고 싶을 때는 어떻게 할까? | 저장(북마크) 기능과 마이페이지 제공 |

---

## 4. 기술 스택

개발 조건: 유료 API 사용 불가 · 소셜 로그인 미사용(이메일 로그인만) · DB는 Supabase로 확정

| 영역 | 사용 기술 | 비고 |
|---|---|---|
| 프론트+백엔드 | Next.js (React) | 프론트와 API Route를 한 프로젝트로 묶어 Vercel에 배포 |
| 인증 | Supabase Auth (이메일/비밀번호) | 소셜 로그인 미사용, 이메일 인증만 구현 |
| DB | Supabase Postgres + pgvector | 이미지 임베딩 벡터 저장 및 유사도 검색 |
| 이미지 임베딩 | CLIP ViT-B/32 + Hugging Face Inference API | 서버 직접 운영 없이 무료 API 호출로 임베딩 추출 |
| 관광지 정보 | 한국관광공사 TourAPI | 공공데이터포털 무료 API 키 |
| 지도 | 카카오맵 JavaScript SDK | 무료 티어 내 사용 |
| 주변 검색 | 카카오 로컬 API | 무료 티어 내 사용 |
| 파일 저장 | Supabase Storage | 사용자 업로드 사진 저장 |
| 배포 | Vercel | Next.js 프로젝트 무료 티어 배포 |

> 배포/데모 목적으로 동일사진 1차 필터(Perceptual Hash) 단계는 생략하고, CLIP 유사도 검색 하나로 구조를 단순화했다.

---

## 5. MVP 정의

와이어프레임 화면을 기준으로, 핵심 가설("사진 한 장으로 장소를 찾아준다")을 검증하는 데 필요한 기능만 MVP로 선정.

| ID | 기능명 | 설명 | 관련 화면 |
|---|---|---|---|
| F-01 | 회원가입 / 로그인 | 이메일·비밀번호 기반 회원가입 및 로그인 | 1a, 1b |
| F-02 | 홈 | 정확 매칭 / 유사 매칭 / 저장함 진입점 제공 | 1c |
| F-03 | 정확히 일치하는 곳 찾기 | 사진 업로드 → 최상위 매칭 1건을 정확도(%)와 함께 반환 (무조건 결과 반환) | 2a, 2b |
| F-04 | 유사한 관광지 찾기 | 사진 업로드 → 상위 매칭 3건을 유사도(%)와 함께 리스트로 반환 | 3a, 3b |
| F-05 | 관광지 상세정보 | 대표이미지·평점·운영시간·입장료·설명 조회 | 4a |
| F-06 | 지도로 보기 | 관광지·먹거리·놀거리 위치를 지도에 마커로 표시 | 5a |
| F-07 | 주변 먹거리·놀거리 | 반경 내 음식점/카페/명소 리스트 제공 | 5b, 5c |
| F-08 | 저장 / 마이페이지 | 관광지·먹거리·놀거리 북마크 및 목록 조회 | 6a |

### MVP 제외 (v2 이후 검토)
- 리뷰·평점 작성 및 커뮤니티 기능
- 사진/장소 공유 및 SNS 연동 게시 기능
- 다국어 지원, 오프라인(다운로드) 지원, 푸시 알림

### 구현 우선순위
1. **1차:** F-03(정확 매칭) + F-05(상세정보) + F-06(지도) → 핵심 가설 최소 검증
2. **2차:** F-04(유사 매칭) + F-07(주변 먹거리·놀거리) → 탐색 경험 확장
3. **3차:** F-01(회원 인증) + F-08(저장/마이페이지) → 재사용·재방문 유도

---

## 6. 상세 기능 명세

### F-01 회원가입 / 로그인
- **입력:** 이름, 이메일, 비밀번호, 비밀번호 확인, 약관 동의 체크
- **처리:** Supabase Auth `signUp()` / `signInWithPassword()` 호출, 세션 토큰 발급
- **출력:** 로그인 성공 시 홈(1c)으로 이동, 실패 시 에러 메시지(이메일 중복, 비밀번호 불일치 등)
- **예외 처리:** 이메일 형식 오류, 비밀번호 8자 미만, 약관 미동의 시 가입 버튼 비활성화

### F-02 홈
- **입력:** 없음 (버튼 클릭 이벤트)
- **처리:** 3개 버튼(정확 매칭/유사 매칭/저장함) 라우팅
- **출력:** 각 기능 화면으로 이동

### F-03 · F-04 이미지 매칭 (공통 엔진)
F-03과 F-04는 같은 매칭 엔진을 사용하고, 반환 개수와 화면 문구만 다르다.

1. 사용자가 업로드한 사진을 Next.js API Route가 받아 Hugging Face Inference API(CLIP ViT-B/32)로 전송
2. Hugging Face로부터 이미지 임베딩(벡터)을 응답받음
3. Supabase pgvector에서 코사인 유사도 검색(`ORDER BY embedding <=> query_embedding`)으로 전체 관광지 이미지 중 상위 후보 조회
4. F-03(정확 매칭): 유사도 최상위 1건만 반환 / F-04(유사 매칭): 상위 3건 반환
5. 코사인 유사도를 0~100% 정확도로 환산해 화면에 표시

> 결과 없음 상태를 두지 않는다 — 유사도가 아무리 낮아도 최상위 후보를 반드시 1건 이상 반환하며, 정확도(%) 자체가 신뢰도를 나타내는 지표이자 완곡한 면책 표시 역할을 한다.

### F-05 관광지 상세정보
- **입력:** `place_id` (또는 `tour_content_id`)
- **처리:** TourAPI `detailCommon2` / `detailIntro2` / `detailImage2` 조회
- **출력:** 대표이미지, 평점, 카테고리, 주소, 운영시간, 입장료, 설명

### F-06 지도로 보기
- **입력:** 관광지 좌표, 반경 내 먹거리·놀거리 좌표 목록
- **처리:** 카카오맵 SDK로 Map 생성, 각 위치에 Marker 표시
- **출력:** 마커 클릭 시 InfoWindow로 이름·거리·카테고리 노출

### F-07 주변 먹거리·놀거리
- **입력:** 관광지 좌표(x, y), 반경(예: 1000m)
- **처리:** 카카오 로컬 API `category.json` 호출, `category_group_code`(FD6/CE7/AT4)로 필터링
- **출력:** `place_name`, `distance`, `category_name` 기반 리스트

### F-08 저장 / 마이페이지
- **입력:** 저장 버튼 클릭 (`place_id`, `type`)
- **처리:** `saved_places` 테이블에 upsert, 마이페이지 진입 시 `user_id` 기준 조회
- **출력:** 저장한 관광지/먹거리/놀거리 탭별 목록

---

## 7. 이미지 매칭 정확도 표시 정책

**핵심 원칙:** 검색 결과는 항상 반환한다. "일치하는 곳이 없습니다"와 같은 실패 상태는 만들지 않고, 정확도(%) 수치와 안내 문구로 신뢰도를 전달한다.

| 정확도 구간 | 판정 | 화면 안내 문구 (예시) |
|---|---|---|
| 90~100% | 정확 일치 | "정확도 OO%로 일치하는 곳을 찾았어요" |
| 70~89% | 일치 가능성 높음 | "정확도 OO%, 이 곳일 가능성이 높아요" |
| 40~69% | 유사한 후보 | "정확도 OO%예요. 정확히 일치하진 않지만 가장 비슷한 곳이에요" |
| 0~39% | 낮은 확신 | "정확도 OO%로 확신은 낮지만, 가장 가까운 결과예요. 실제 장소와 다를 수 있어요" |

**정확도 산출 방식**
- CLIP 코사인 유사도(원점수 대략 0.5~1.0 분포)를 0~100% 스케일로 선형 변환
- 변환식(초기안): `정확도(%) = clamp((cosine유사도 - 0.5) / (1.0 - 0.5) × 100, 0, 100)` — 실제 데이터 분포를 보며 보정 필요
- F-03 화면에는 정확도 구간과 무관하게 항상 상위 1건 + 정확도(%)를 노출
- F-04 화면에는 상위 3건을 정확도(%) 내림차순으로 나열
- 40% 미만 구간은 화면 하단에 공통 면책 문구를 상시 노출: "정확도가 낮을 경우 실제 장소와 다를 수 있어요"

---

## 8. Supabase 데이터베이스 설계

| 테이블 | 주요 컬럼 | 설명 |
|---|---|---|
| `auth.users` | `id`, `email` | Supabase Auth 기본 제공 테이블 |
| `places` | `id`, `tour_content_id`, `name`, `category`, `address`, `rating`, `open_hours`, `price` | TourAPI 기준 관광지 마스터 정보 |
| `place_images` | `id`, `place_id`, `image_url`, `phash` | 관광지별 대표/참고 이미지 및 pHash |
| `place_embeddings` | `id`, `place_id`, `embedding vector(512)` | pgvector 컬럼, CLIP 임베딩 저장 |
| `saved_places` | `id`, `user_id`, `place_id`, `type`, `created_at` | 사용자 저장(북마크), `type`: 관광지/먹거리/놀거리 |
| `search_logs` | `id`, `user_id`, `uploaded_image_url`, `matched_place_id`, `accuracy`, `created_at` | 검색 이력 기록, 향후 매칭 정확도 개선용(선택) |

- 모든 테이블에 Row Level Security(RLS) 적용 — `saved_places`, `search_logs`는 `user_id` 본인 데이터만 조회/수정 가능
- `place_embeddings`는 pgvector 익스텐션 활성화 후 `ivfflat` 인덱스 생성으로 검색 속도 확보

---

## 9. 비기능 요구사항

- **성능:** 이미지 업로드 후 매칭 결과 반환까지 3초 이내 목표 (Hugging Face Inference API 응답 속도 기준)
- **보안:** Supabase RLS로 사용자별 데이터 접근 제한, 이미지 업로드 용량 제한(예: 10MB)
- **확장성:** 임베딩 모델(CLIP)을 인터페이스로 분리해 추후 더 정확한 모델로 교체 가능하도록 설계
- **비용:** 전 구간 무료 티어(Supabase, TourAPI, 카카오맵/로컬 API, Hugging Face Inference API, Vercel) 내에서 동작하도록 설계, 무료 호출 한도 초과 시 알림 로직 추가 권장

---

## 10. 배포 스택 및 환경변수

| 항목 | 선택 |
|---|---|
| 프론트+백엔드 | Next.js (React) — API Route로 백엔드 로직 포함, Vercel에 단일 배포 |
| 이미지 임베딩 | CLIP ViT-B/32 (`openai/clip-vit-base-patch32`), Hugging Face Inference API로 호출 |
| 벡터 DB | Supabase Postgres + pgvector |
| 지도 | 카카오맵 JavaScript SDK |
| 주변 정보 | 카카오 로컬 API |
| 관광지 정보 | 한국관광공사 TourAPI |
| 배포 | Vercel |

**필요 환경변수** (`.env.local.example` 참고)
- `HUGGINGFACE_API_TOKEN` — huggingface.co 무료 계정에서 발급
- `SUPABASE_URL`, `SUPABASE_ANON_KEY`
- `KAKAO_MAP_KEY`, `KAKAO_REST_API_KEY`
- `TOUR_API_KEY` — 공공데이터포털 발급

**요청 흐름**
1. React 프론트에서 사진 업로드 → Next.js API Route(`/api/match`) 호출
2. API Route가 Hugging Face Inference API로 CLIP 임베딩 요청
3. 받은 임베딩으로 Supabase pgvector 쿼리 실행 → 정확도(%) 계산 후 결과 반환
